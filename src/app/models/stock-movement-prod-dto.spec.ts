import { StockMovementProdDTO } from './stock-movement-prod-dto';

describe('StockMovementProdDTO', () => {
  it('should create an instance', () => {
    expect(new StockMovementProdDTO()).toBeTruthy();
  });
});
