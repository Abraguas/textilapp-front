import { ClientRegisterDTO } from './client-register-dto';

describe('ClientRegisterDTO', () => {
  it('should create an instance', () => {
    expect(new ClientRegisterDTO()).toBeTruthy();
  });
});
