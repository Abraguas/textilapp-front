import { EarningsPerMonthDTO } from './earnings-per-month-dto';

describe('EarningsPerMonthDTO', () => {
  it('should create an instance', () => {
    expect(new EarningsPerMonthDTO()).toBeTruthy();
  });
});
