import { HighestSellingProductDTO } from './highest-selling-product-dto';

describe('HighestSellingProductDTO', () => {
  it('should create an instance', () => {
    expect(new HighestSellingProductDTO()).toBeTruthy();
  });
});
